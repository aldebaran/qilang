package tests

type AudioBuffer struct
  size   uint32
  lock   Mutex
  buffer []uint32
end

interface AudioDevice
  fn setDefaultDevice (a str, b str) a:int

  in start
  in stop
  in mute
  in unmute

  out started
  out stopped
  out muted
  out unmuted
end

interface Adder<T>
  in   add T
  prop value T
end

interface Adder
  in   add(int)
  prop value(int)
end
