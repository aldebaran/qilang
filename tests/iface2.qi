package tests

from tutu import *
from tutu import toto
from tutu import tutu, toto

#comment
#comment
interface AudioDevice
  fn setDefaultDevice (str, str) int

  fn start()
  fn stop()
  fn mute()
  fn unmute()

  emit started(cacaca, dsldslksdflk)
  emit stopped(prout)
  emit muted()
  emit unmuted()

  prop velocity(int)
end
